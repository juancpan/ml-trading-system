# Core ML / numeric stack — pinned and tested together.
# NOTE: tensorflow and ml_dtypes MUST be upgraded as a pair
# (ml_dtypes < 0.5.1 lacks float4_e2m1fn, which raises AttributeError
# at interpreter teardown with tensorflow 2.19).
ml_dtypes==0.5.4
tensorflow==2.19.0
keras==3.9.0
keras-tcn==3.5.6
numpy==2.1.3
scipy==1.15.2
scikit-learn==1.6.1
statsmodels==0.14.4
pandas==2.2.3
pyzmq==27.0.0

# Runtime dependencies — used by the research core and/or execution layer.
# Tested with recent versions; not hard-pinned.
yfinance            # data download fallback (clean-clone quickstart)
xgboost             # gradient-boosted tree models
matplotlib          # backtest plots
torch               # imported by algos/common/config.py (seed management)
pytz
PyYAML
python-dateutil
joblib
requests
pmdarima            # ARIMA order selection
exchange-calendars  # per-exchange market calendars/holidays
holidays

# Interactive Brokers native API (execution layer only — not needed for backtests)
ibapi

# Testing
pytest
